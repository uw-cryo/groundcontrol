# review-base

Intentionally (almost) empty branch. It exists only so a PR from `plan-review`
shows the **entire** `docs/plan.md` as an added diff — making every line
commentable inline (including the schema table). Do not merge review PRs here.
